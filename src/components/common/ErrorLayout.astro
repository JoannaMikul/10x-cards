---
import Layout from "../../layouts/Layout.astro";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <main class="min-h-[calc(100vh-3rem)] w-full flex items-center justify-center bg-background">
    <slot />
  </main>
</Layout>
